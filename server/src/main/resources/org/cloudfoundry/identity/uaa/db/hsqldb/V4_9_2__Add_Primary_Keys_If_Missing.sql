alter table OAUTH_CODE add column id INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY;
alter table EXTERNAL_GROUP_MAPPING add column id INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY;
alter table SEC_AUDIT add column id INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY;
alter table GROUP_MEMBERSHIP add column id INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY;
