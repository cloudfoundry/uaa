participant vmc
participant cc
participant cs

vmc->cc: bind_app_to_service(token, org, proj, appname, svcname)
cc->cs: check_authorized(token, org, proj, [(u, app, appname), (u, service, svcname)])
note over cs:
    get ACLs for org, proj
    check_authorized(token, acls, (perm, restype, resname)[])
end note
cs->cc: if yes, resid[]
note over cc: if yes, bind resid[0] => appID, to resid[1] => svcID
cc->vmc:
