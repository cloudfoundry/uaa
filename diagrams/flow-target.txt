participant vmc
participant cc
participant cs

note over vmc: $ target cfinstance/org/proj
vmc->cc: get_info(org, proj)
cc->cs: get_info(org, proj)
cs->cc: cs_info
cc->vmc: cs_info + cc_info
note over vmc: if info is valid, save target
