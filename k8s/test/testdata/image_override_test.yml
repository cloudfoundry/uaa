#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.subset({"kind":"Deployment"})
---
spec:
  template:
    spec:
      containers:
      #@overlay/match by="name"
      - name: "uaa"
        #@overlay/assert
        image: cfidentity/uaa@sha256:other
